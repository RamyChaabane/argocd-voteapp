{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "config:base"
  ],
  "enabledManagers": ["regex"],
  "regexManagers": [
    {
      "fileMatch": ["kustomize/dev/kustomization\\.ya?ml$"],
      "matchStrings": [
        "newName:\\s*(?<depName>ramydockerid/vote-[^\\s]+)[\\s\\S]*?newTag:\\s*(?<currentValue>[\\w.\\-]+)"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "fileMatch": ["kustomize/staging/kustomization\\.ya?ml$"],
      "matchStrings": [
        "newName:\\s*(?<depName>ramydockerid/vote-[^\\s]+)[\\s\\S]*?newTag:\\s*(?<currentValue>[\\w.\\-]+)"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "fileMatch": ["kustomize/prod/kustomization\\.ya?ml$"],
      "matchStrings": [
        "newName:\\s*(?<depName>ramydockerid/vote-[^\\s]+)[\\s\\S]*?newTag:\\s*(?<currentValue>[\\w.\\-]+)"
      ],
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "matchPaths": ["kustomize/dev/.*"],
      "labels": ["env:dev"]
    },
    {
      "matchPaths": ["kustomize/staging/.*"],
      "labels": ["env:staging"]
    },
    {
      "matchPaths": ["kustomize/prod/.*"],
      "labels": ["env:prod"]
    }
  ]
}
